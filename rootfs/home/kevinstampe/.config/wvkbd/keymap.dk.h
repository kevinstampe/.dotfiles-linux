/* The LATIN xkb keymap wvkbd embeds is ~400KB of escaped string. There is no
 * reason to fork it: this layout set uses "latin" for every layer, so just
 * reuse upstream's keymaps verbatim.
 */
#include "keymap.mobintl.h"
